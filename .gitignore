# Media is served from Google Cloud Storage (see upload_assets.py), not from
# this repo: assets/ is generated by collect_assets.py and stays local, cache
# included. data.js and tables.js ARE committed — the page is served from here.
assets/
__pycache__/
